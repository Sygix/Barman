module.exports = class createEmbed{

    /*static createR6(response, type){
        if(type == 'op'){
            var embed = new Discord.RichEmbed()
                .setTitle('Stats des opérateurs')
                .setColor('#d00e51');
            for(){

            }
            embed.setURL('https://r6stats.com');
        }
        if(type == 'saison'){
            var embed = new Discord.RichEmbed()
                .setTitle('Stats en classé')
                .setColor('#d00e51');
            for(var i = 0; i < response.seasons.; i++){
                embed.addField(response.seasons[i].emea.wins);
            }
            embed.setURL('https://r6stats.com');
        }
        if(type == null){
            var embed = new Discord.RichEmbed()
                .setTitle('Stats pour ' + response.player.username)
                .setColor('#d00e51')
                .setThumbnail("https://www.windowscentral.com/sites/wpcentral.com/files/topic_images/2018/rainbow-six-siege-logo.png")
                .setDescription('blabla')
                .setURL('https://r6stats.com')
                .setTimestamp(new Date(response.player.updated_at));
        }
        return {embed};
    }*/

};